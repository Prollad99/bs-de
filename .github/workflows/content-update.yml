name: Aktualisiere Links und Bereitstellung

on:
  schedule:
    - cron: '*/30 * * * *' # Läuft alle 30 Minuten
  push:
    branches:
      - main

jobs:
  links-aktualisieren:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        game:
          - coin-master
          - monopoly-go
          - bingo-blitz
          - dice-dreams
          - match-masters
          - solitaire-grand-harvest
          - slotpark
          - house-of-fun
          - slotomania
          - bingo-bash
          - huuuge-casino
          - wsop
          - crazy-coin
          - gametwist

    steps:
      - name: Repository auschecken
        uses: actions/checkout@v2

      - name: Node-Module zwischenspeichern
        uses: actions/cache@v2
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Abhängigkeiten installieren
        run: npm install

      # Install Playwright Browsers
      - name: Installiere Playwright-Browser
        run: npx playwright install

      - name: Links abrufen und aktualisieren
        run: |
          node index-js/${{ matrix.game }}.js
          echo "Inhalt von links-json/${{ matrix.game }}.json:"
          cat links-json/${{ matrix.game }}.json
          echo "Inhalt von _includes/${{ matrix.game }}.html:"
          cat _includes/${{ matrix.game }}.html

      - name: Dynamische Beitragsinhalte generieren
        run: |
          SPIEL_NAME=${{ matrix.game }}
          SPIEL_NAME_KAPITALISIERT=$(echo "$SPIEL_NAME" | sed 's/-/ /g' | awk '{for(i=1;i<=NF;i++) $i=toupper(substr($i,1,1)) substr($i,2)}1')

          ABSATZ_DATEI="_includes/${SPIEL_NAME}_post.html"
          ABSATZ="Willkommen zu den neuesten ${SPIEL_NAME_KAPITALISIERT}-Belohnungen!\n\n
          Suchst du nach den aktuellsten ${SPIEL_NAME_KAPITALISIERT}-Freispielen, Münzen oder Bonusangeboten? Du bist hier genau richtig! Wir aktualisieren diese Seite täglich mit den neuesten Belohnungslinks, damit du das Beste aus deinem Spielerlebnis herausholen kannst.\n\n
          **Warum du täglich vorbeischauen solltest:**\n
          - 🏆 **Täglich neue Links** – Verpasse keine Belohnung mehr.\n
          - ⏳ **Einfache Einlösung** – Ein Klick und du bekommst deine Belohnung.\n
          - 🎯 **Sichere & offizielle Quellen** – Keine Hacks oder Cheats, nur legitime Links.\n\n
          ### Tipps für mehr Belohnungen in ${SPIEL_NAME_KAPITALISIERT}:\n
          - Spiele regelmäßig und halte Ausschau nach Bonus-Events.\n
          - Verbinde dein Spiel mit sozialen Medien, um zusätzliche Prämien zu erhalten.\n
          - Nutze exklusive Angebote während Feiertagen oder speziellen Events.\n\n
          **Hinweis:** Diese Belohnungen stammen aus offiziellen ${SPIEL_NAME_KAPITALISIERT}-Quellen und sind völlig kostenlos. Wir sind keine offizielle Partnerseite von ${SPIEL_NAME_KAPITALISIERT}, sondern eine unabhängige Plattform, die Spielern hilft, das Beste aus ihrem Spiel herauszuholen."
          echo -e "$ABSATZ" > "$ABSATZ_DATEI"

      - name: Dynamische Footer-Inhalte generieren
        run: |
          SPIEL_NAME=${{ matrix.game }}
          SPIEL_NAME_KAPITALISIERT=$(echo "$SPIEL_NAME" | sed 's/-/ /g' | awk '{for(i=1;i<=NF;i++) $i=toupper(substr($i,1,1)) substr($i,2)}1')

          FOOTER_DATEI="_includes/${SPIEL_NAME}_footer.html"
          FOOTER="### So holst du dir deine ${SPIEL_NAME_KAPITALISIERT}-Belohnungen\n\n
          Das Einfordern deiner täglichen Belohnungen in ${SPIEL_NAME_KAPITALISIERT} ist schnell und einfach! Folge diesen Schritten:\n
          1️⃣ **Belohnungslink auswählen** – Klicke auf einen der oben aufgeführten Links.\n
          2️⃣ **Spiel öffnen** – Der Link leitet dich zur ${SPIEL_NAME_KAPITALISIERT}-App oder Website weiter.\n
          3️⃣ **Belohnung erhalten** – Logge dich ein und erhalte deine Freispiele oder Münzen.\n\n
          **💡 Nützliche Hinweise:**\n
          - Jeder Link ist zeitlich begrenzt – löse deine Belohnungen möglichst schnell ein.\n
          - Stelle sicher, dass du mit dem richtigen ${SPIEL_NAME_KAPITALISIERT}-Konto eingeloggt bist.\n
          - Falls ein Link nicht funktioniert, könnte er bereits abgelaufen sein – schau regelmäßig vorbei für neue Links.\n\n
          **Noch mehr Boni?**\n
          - Tritt der ${SPIEL_NAME_KAPITALISIERT}-Community bei und tausche dich mit anderen Spielern aus.\n
          - Folge den offiziellen ${SPIEL_NAME_KAPITALISIERT}-Social-Media-Kanälen, um exklusive Events nicht zu verpassen.\n\n
          Genieße dein Spiel und viel Spaß mit den täglichen ${SPIEL_NAME_KAPITALISIERT}-Belohnungen! 🎉"
          echo -e "$FOOTER" > "$FOOTER_DATEI"

      - name: Letztes Änderungsdatum der Include-Datei aktualisieren
        run: |
          export TZ=UTC
          ZEITSTEMPEL=$(date -u +"%Y-%m-%d %H:%M:%S")
          INCLUDE_FILES_YML="_data/include_files.yml"

          if git diff --quiet _includes/${{ matrix.game }}.html; then
            echo "_includes/${{ matrix.game }}.html wurde nicht geändert. Aktualisierung des letzten Änderungsdatums übersprungen."
          else
            if grep -q "${{ matrix.game }}.html:" "$INCLUDE_FILES_YML"; then
              sed -i "/${{ matrix.game }}.html:/,/last_modified:/s|last_modified: .*|last_modified: $ZEITSTEMPEL|" "$INCLUDE_FILES_YML"
            else
              echo -e "\n${{ matrix.game }}.html:\n  last_modified: $ZEITSTEMPEL" >> "$INCLUDE_FILES_YML"
            fi
          fi

      - name: Git konfigurieren
        run: |
          git config --global user.email "prolladmail@gmail.com"
          git config --global user.name "Prollad99"

      - name: Änderungen committen und pushen
        env:
          ACTIONS_DEPLOY_KEY: ${{ secrets.ACTIONS_DEPLOY_KEY }}
        run: |
          git add _includes/${{ matrix.game }}_post.html _includes/${{ matrix.game }}_footer.html
          git commit -m "Aktualisierte Inhalte für ${SPIEL_NAME_KAPITALISIERT} zur Verbesserung der AdSense-Bewertung"
          git push https://x-access-token:${{ secrets.ACTIONS_DEPLOY_KEY }}@github.com/Prollad99/bs-de.git || true