name: Aktualisiere Links und Bereitstellung

on:
  schedule:
    - cron: '*/30 * * * *' # Läuft alle 30 Minuten
  push:
    branches:
      - main

jobs:
  links-aktualisieren:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        game:
          - coin-master
          - monopoly-go
          - bingo-blitz
          - dice-dreams
          - match-masters
          - solitaire-grand-harvest
          - slotpark
          - house-of-fun
          - slotomania
          - bingo-bash
          - huuuge-casino
          - wsop
          - crazy-coin
          - gametwist

    steps:
      - name: Repository auschecken
        uses: actions/checkout@v2

      - name: Node-Module zwischenspeichern
        uses: actions/cache@v2
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Abhängigkeiten installieren
        run: npm install

      - name: Installiere Playwright-Browser
        run: npx playwright install

      - name: Links abrufen und aktualisieren
        run: |
          node index-js/${{ matrix.game }}.js
          echo "Inhalt von links-json/${{ matrix.game }}.json:"
          cat links-json/${{ matrix.game }}.json

      - name: Dynamische Footer-Inhalte generieren
        run: |
          SPIEL_NAME=${{ matrix.game }}
          SPIEL_NAME_KAPITALISIERT=$(echo "$SPIEL_NAME" | sed 's/-/ /g' | awk '{for(i=1;i<=NF;i++) $i=toupper(substr($i,1,1)) substr($i,2)}1')

          FOOTER_DATEI="_includes/${SPIEL_NAME}_footer.html"

          FOOTER="### (Diese Liste wird täglich aktualisiert – speichere die Seite und schaue regelmäßig vorbei!)

          🏆 Profi-Tipps für ${SPIEL_NAME_KAPITALISIERT} Spieler
          - **Nutze Belohnungen strategisch**: Warte auf knifflige Spielmomente, um Boni einzulösen.
          - **Verbinde dich mit Freunden**: Viele Spiele bieten Extra-Belohnungen für das Einladen von Freunden.
          - **Bleibe auf dem Laufenden**: Folge offiziellen ${SPIEL_NAME_KAPITALISIERT}-Social-Media-Seiten für weitere Geheimtipps.

          ❓ Häufig gestellte Fragen (FAQ)
          - **Wie oft kann ich die Belohnungslinks nutzen?**  
            👉 Jeder Link ist normalerweise nur einmal gültig. Täglich neue Links nutzen!

          - **Warum funktioniert ein Link nicht?**  
            👉 Einige Links haben eine begrenzte Laufzeit. Falls ein Link abgelaufen ist, probiere die neuesten aus.

          📌 **Fazit**  
          Vergiss nicht, täglich zurückzukommen, um neue Belohnungen zu erhalten! Speichere diese Seite oder teile sie mit deinen Freunden. Viel Spaß beim Spielen von ${SPIEL_NAME_KAPITALISIERT}!"

          echo -e "$FOOTER" > "$FOOTER_DATEI"

      - name: Letztes Änderungsdatum der Include-Datei aktualisieren
        run: |
          export TZ=UTC
          ZEITSTEMPEL=$(date -u +"%Y-%m-%d %H:%M:%S")
          INCLUDE_FILES_YML="_data/include_files.yml"

          if git diff --quiet _includes/${{ matrix.game }}_footer.html; then
            echo "_includes/${{ matrix.game }}_footer.html wurde nicht geändert. Aktualisierung des letzten Änderungsdatums übersprungen."
          else
            if grep -q "${{ matrix.game }}_footer.html:" "$INCLUDE_FILES_YML"; then
              sed -i "/${{ matrix.game }}_footer.html:/,/last_modified:/s|last_modified: .*|last_modified: $ZEITSTEMPEL|" "$INCLUDE_FILES_YML"
            else
              echo -e "\n${{ matrix.game }}_footer.html:\n  last_modified: $ZEITSTEMPEL" >> "$INCLUDE_FILES_YML"
            fi
          fi

      - name: Git konfigurieren
        run: |
          git config --global user.email "prolladmail@gmail.com"
          git config --global user.name "Prollad99"

      - name: Änderungen committen und pushen
        env:
          ACTIONS_DEPLOY_KEY: ${{ secrets.ACTIONS_DEPLOY_KEY }}
        run: |
          git add _includes/${{ matrix.game }}_footer.html _data/include_files.yml
          git add package.json package-lock.json || true
          git commit -m "Aktualisiere ${SPIEL_NAME}-Footer mit verbesserten Inhalten" || true
          git stash
          git pull --rebase || true
          git stash pop || true
          for i in {1..5}; do
            git push https://x-access-token:${{ secrets.ACTIONS_DEPLOY_KEY }}@github.com/Prollad99/bs-de.git && break
            echo "Versuch $i: Push fehlgeschlagen, erneuter Versuch in 5 Sekunden..."
            sleep 5
            git pull --rebase || true
          done